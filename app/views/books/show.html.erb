<p style="color: green">
  <%= notice %>
</p>
<%= render @book %>
  <nav class="page-nav">
    <%= link_to t('book.notice.edit_this_book'), edit_book_path(@book) %> |
      <%= link_to t('book.notice.back_to_books') , books_path %>
        <%= button_to t('book.notice.destroy_this_book') , @book, method: :delete %>
  </nav>
