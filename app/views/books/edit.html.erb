<h1>
  <%= t('.editing_book', model: Book.model_name.human(count: 1)) %>
</h1>
<%= render "form" , book: @book %>
  <br>
  <nav class="page-nav">
    <%= link_to t('.show_this_book', model: Book.model_name.human(count: 1)) , @book %> |
      <%= link_to t('.back_to_books', model: Book.model_name.human(count: 1)) , books_path %>
  </nav>
